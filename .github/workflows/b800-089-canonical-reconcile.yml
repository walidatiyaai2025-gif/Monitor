name: b800-089-canonical-reconcile

on:
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/b800-089-canonical-reconcile.yml

permissions:
  contents: write

jobs:
  reconcile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: agent/b800-089-performance-health-export
          fetch-depth: 0
      - name: Reconcile B800-089 canonical documentation with guarded replacements
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          def replace_once(path: str, old: str, new: str) -> None:
              p = Path(path)
              text = p.read_text(encoding="utf-8")
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f"{path}: expected exactly one guarded match, found {count}")
              p.write_text(text.replace(old, new, 1), encoding="utf-8")

          batch = "docs/BATCH_800.md"
          replace_once(
              batch,
              "| Performance | cached health-module read model + bounded cumulative wait evidence | **B800 wait-stat projection wired and SQL-real validated on pre-canonical heads** |",
              "| Performance | cached health-module read model + bounded cumulative wait evidence | **B800 wait-stat projection wired and SQL-real validated on pre-canonical heads; B800-089 adds an estate-wide Viewer+ cached Performance Health summary CSV with anonymous top wait category/score evidence and no concrete wait identity** |")
          replace_once(
              batch,
              "| Reports | versioned CSV/ZIP/JSON endpoints | **Existing exports remain wired; B800-081 adds a Viewer+ bounded/versioned/redacted Fleet decision-support CSV, B800-082 adds a Viewer+ per-server/per-operation Maintenance decision-support CSV that keeps target identity request-only and unavailable evidence explicit, B800-083 adds a contextual Viewer+ cached Server Intelligence CSV, B800-084 adds a contextual Viewer+ cached Database Health summary CSV, B800-085 completes their selected-Server-Details download workflow, B800-086 adds a contextual Viewer+ cached Memory Health summary CSV, B800-087 adds an estate-wide Viewer+ cached Backup Health aggregate CSV with RPO compliance deliberately `NotEvaluated`, and B800-088 adds an estate-wide Viewer+ cached SQL Agent Health aggregate CSV with anonymous reliability metrics and schedule lateness deliberately `NotEvaluated`** |",
              "| Reports | versioned CSV/ZIP/JSON endpoints | **Existing exports remain wired; B800-081 adds a Viewer+ bounded/versioned/redacted Fleet decision-support CSV, B800-082 adds a Viewer+ per-server/per-operation Maintenance decision-support CSV that keeps target identity request-only and unavailable evidence explicit, B800-083 adds a contextual Viewer+ cached Server Intelligence CSV, B800-084 adds a contextual Viewer+ cached Database Health summary CSV, B800-085 completes their selected-Server-Details download workflow, B800-086 adds a contextual Viewer+ cached Memory Health summary CSV, B800-087 adds an estate-wide Viewer+ cached Backup Health aggregate CSV with RPO compliance deliberately `NotEvaluated`, B800-088 adds an estate-wide Viewer+ cached SQL Agent Health aggregate CSV with anonymous reliability metrics and schedule lateness deliberately `NotEvaluated`, and B800-089 adds an estate-wide Viewer+ cached Performance Health summary CSV with workload aggregates plus anonymous top B400 wait category/score evidence** |")
          replace_once(
              batch,
              "\n\n## Task program",
              "\n\nB800-089 extends the bounded report family to estate-wide cached Performance Health without creating a second performance or wait truth model. Viewer+ `GET /reports/performance-health.csv` reads enabled registrations plus `IServerHealthSnapshotCache.Peek(...)` only, serializes current active requests, runnable tasks and pending I/O aggregates, and reuses `WaitIntelligenceProjection.Build(performance, uptime, 1)` for the anonymous highest existing B400 wait category/score/severity/rate/share/signal summary. Concrete wait type/fingerprint and registration IDs are excluded. Missing cache/performance/wait evidence remains explicit `Unavailable`; legitimate observed workload zeroes remain zero. The wait evidence remains cumulative since SQL Server start and uptime-normalized rather than interval history. Implementation head `01dd1780c5b84137a41bb4e262bc36a767bc1bde` passed CI #2790 / `32081864141` and Windows production-candidate #513 / `32081864132`; Real SQL was not selected because the slice changes no monitored-SQL query, collector or permission path. No wait reset, tuning, refresh, mutation, remediation, failover or configuration write is introduced.\n\n## Task program")
          replace_once(
              batch,
              "- [x] B800-088 add a Viewer+ bounded/versioned estate-wide cached SQL Agent Health summary CSV from enabled registrations + snapshot-cache `Peek(...)`; reuse anonymous `AgentReliabilityProjection` metrics, preserve missing history/activity as `Unavailable`, keep schedule lateness `NotEvaluated`, and exclude job keys/owners/next-run timestamps/registration IDs without monitored-SQL/refresh/Agent execution/mutation (`docs/work/B800-088.md`, PR #323).\n- [ ] B800-089..090 continue bounded, versioned, redacted exports only where existing evidence and operator value justify them.",
              "- [x] B800-088 add a Viewer+ bounded/versioned estate-wide cached SQL Agent Health summary CSV from enabled registrations + snapshot-cache `Peek(...)`; reuse anonymous `AgentReliabilityProjection` metrics, preserve missing history/activity as `Unavailable`, keep schedule lateness `NotEvaluated`, and exclude job keys/owners/next-run timestamps/registration IDs without monitored-SQL/refresh/Agent execution/mutation (`docs/work/B800-088.md`, PR #323 merged as `88bbd3bf22c99a5cd0ce6e762c4c0383dddd7445`).\n- [x] B800-089 add a Viewer+ bounded/versioned estate-wide cached Performance Health summary CSV from enabled registrations + snapshot-cache `Peek(...)`; preserve workload zeroes, keep missing wait evidence `Unavailable`, reuse anonymous top B400 wait category/score evidence and exclude concrete wait identity/registration IDs without monitored-SQL/refresh/tuning/mutation (`docs/work/B800-089.md`, PR #324).\n- [ ] B800-090 close the reports/exports tranche with one final bounded evidence-backed report slice or explicit no-more-export closeout, without inventing unsupported truth.")
          replace_once(
              batch,
              "\n\n## Validation chronology",
              "\n\nBounded cached Performance Health estate export slice:\n- `src/Monitor.Web/Services/PerformanceHealthSummaryExport.cs`\n- `src/Monitor.Web/Services/EnterpriseReportingServices.cs`\n- `src/Monitor.Web/Controllers/EnterpriseReportsController.cs`\n- `src/Monitor.Web/Services/EnterpriseSecurityPolicy.cs`\n- `src/Monitor.Web/Views/Portal/Reports.cshtml`\n- `tests/Monitor.Web.Tests/B800PerformanceHealthSummaryExportTests.cs`\n- `docs/work/B800-089.md`\n\n## Validation chronology")
          replace_once(
              batch,
              "- B800-088 implementation head `ea87e414f97060e9156fd762d407c8e815c26103` passed CI #2772 / `32079766599` and Windows production-candidate #508 / `32079766682` end-to-end; Real SQL was not selected because no monitored-SQL query, collector or permission path changed. Exact final post-canonical-reconciliation validation remains required.\n\n## Current B800-088 merge gate\n\nPR #323 is the current focused B800 slice and closes B800-088 only. `docs/FEATURE_CATALOG.md`, `docs/STATUS.md`, `docs/IMPLEMENTATION_PLAN.md`, and this ledger must describe B800-087 / PR #322 as merged and B800-088 as the bounded cached SQL Agent Health estate export slice. Before PR #323 is marked Ready or merged, every repository-selected required workflow must be Green on the same exact final reconciled head, the branch must remain current with `main`, review threads must remain resolved, and the effective diff must stay bounded to B800-088 implementation/tests/work-note plus canonical reconciliation. Real SQL is required only if repository path policy selects it. Issue #287 remains OPEN for B800-089+.",
              "- B800-088 implementation head `ea87e414f97060e9156fd762d407c8e815c26103` passed CI #2772 / `32079766599` and Windows production-candidate #508 / `32079766682` end-to-end; Real SQL was not selected because no monitored-SQL query, collector or permission path changed. Exact final reconciled head `b612e2f5d0a0194df39fcea235ef0d5882e2873a` passed CI #2780 / `32080772859`, Real SQL #362 / `32080772786`, and Windows production-candidate #512 / `32080772801`; PR #323 squash-merged as `88bbd3bf22c99a5cd0ce6e762c4c0383dddd7445`.\n- B800-089 implementation head `01dd1780c5b84137a41bb4e262bc36a767bc1bde` passed CI #2790 / `32081864141` and Windows production-candidate #513 / `32081864132` end-to-end; Real SQL was not selected because no monitored-SQL query, collector or permission path changed. Exact final post-canonical-reconciliation validation remains required.\n\n## Current B800-089 merge gate\n\nPR #324 is the current focused B800 slice and closes B800-089 only. `docs/FEATURE_CATALOG.md`, `docs/STATUS.md`, `docs/IMPLEMENTATION_PLAN.md`, and this ledger now describe B800-088 / PR #323 as merged and B800-089 as the bounded cached Performance Health estate export slice. Before PR #324 is marked Ready or merged, every repository-selected required workflow must be Green on the same exact final reconciled head, the branch must remain current with `main`, review threads must remain resolved, and the effective diff must stay bounded to B800-089 implementation/tests/work-note plus canonical reconciliation. Real SQL is required only if repository path policy selects it. Issue #287 remains OPEN for B800-090+.")

          catalog = "docs/FEATURE_CATALOG.md"
          replace_once(
              catalog,
              "| Functional operator screen wiring | BATCH-800 | In progress / PR #323 | Issue #287 remains OPEN. Evidence-backed slices are merged through B800-087. Current main includes cached B300 server intelligence, bounded database/memory/wait/logical-I/O/Agent evidence, protected workflow/navigation/role contracts, B800-071 fleet routing/correlation, B800-072 maintenance decision support, B800-073 bounded incident decision evidence, B800-074 repository-bounded incident operator reads, B800-075 native persisted/decorated reads, B800-076 Fleet operator-policy availability, B800-077 Maintenance operator-policy availability, B800-078 bounded Fleet incident risk, B800-079 full bounded Fleet routing coverage, B800-080 full bounded Fleet correlation coverage, B800-081 bounded Fleet decision-support export, B800-082 bounded Maintenance decision-support export, B800-083 bounded cached Server Intelligence export, B800-084 bounded cached Database Health summary export, B800-085 contextual export workflow completion, B800-086 bounded cached Memory Health summary export and B800-087 bounded cached Backup Health estate export. PR #323 carries B800-088 bounded cached SQL Agent Health estate export plus canonical reconciliation. Unsupported dimensions remain explicit; no RC.61/#116/#111 implication |",
              "| Functional operator screen wiring | BATCH-800 | In progress / PR #324 | Issue #287 remains OPEN. Evidence-backed slices are merged through B800-088. Current main includes cached B300 server intelligence, bounded database/memory/wait/logical-I/O/Agent evidence, protected workflow/navigation/role contracts, B800-071 fleet routing/correlation, B800-072 maintenance decision support, B800-073 bounded incident decision evidence, B800-074 repository-bounded incident operator reads, B800-075 native persisted/decorated reads, B800-076 Fleet operator-policy availability, B800-077 Maintenance operator-policy availability, B800-078 bounded Fleet incident risk, B800-079 full bounded Fleet routing coverage, B800-080 full bounded Fleet correlation coverage, B800-081 bounded Fleet decision-support export, B800-082 bounded Maintenance decision-support export, B800-083 bounded cached Server Intelligence export, B800-084 bounded cached Database Health summary export, B800-085 contextual export workflow completion, B800-086 bounded cached Memory Health summary export, B800-087 bounded cached Backup Health estate export and B800-088 bounded cached SQL Agent Health estate export. PR #324 carries B800-089 bounded cached Performance Health estate export plus canonical reconciliation. Unsupported dimensions remain explicit; no RC.61/#116/#111 implication |")
          replace_once(
              catalog,
              "| Bounded cached SQL Agent Health estate export | BATCH-800 / B800-088 | In validation / PR #323 | Viewer+ estate-wide `GET /reports/sql-agent-health.csv` reads enabled registration/control-plane state plus `IServerHealthSnapshotCache.Peek(...)` only, exports bounded cached aggregate job counts, explicit history/activity availability, anonymous highest existing `AgentReliabilityProjection` risk metrics and current activity/running counts through `monitor-export-v2`, preserves missing/cache-read-failed evidence as `Unavailable`, and keeps schedule lateness `NotEvaluated` because canonical server time-zone + recurrence/expected-run semantics are not collected. Job keys/names, owners, individual next-run timestamps and registration IDs are excluded. Implementation head `ea87e414f97060e9156fd762d407c8e815c26103` passed CI #2772 / `32079766599` and Windows production-candidate #508 / `32079766682`; Real SQL was not selected because no monitored-SQL query/collector/permission path changed; exact final post-canonical-reconciliation gate remains required. No Agent execution/refresh/mutation/remediation authority |",
              "| Bounded cached SQL Agent Health estate export | BATCH-800 / B800-088 | Complete / merged | Viewer+ estate-wide `GET /reports/sql-agent-health.csv` reads enabled registration/control-plane state plus `IServerHealthSnapshotCache.Peek(...)` only, exports bounded cached aggregate job counts, explicit history/activity availability, anonymous highest existing `AgentReliabilityProjection` risk metrics and current activity/running counts through `monitor-export-v2`, preserves missing/cache-read-failed evidence as `Unavailable`, and keeps schedule lateness `NotEvaluated` because canonical server time-zone + recurrence/expected-run semantics are not collected. Job keys/names, owners, individual next-run timestamps and registration IDs are excluded. Exact final reconciled head `b612e2f5d0a0194df39fcea235ef0d5882e2873a` passed CI #2780 / `32080772859`, Real SQL #362 / `32080772786`, and Windows production-candidate #512 / `32080772801`; PR #323 squash-merged as `88bbd3bf22c99a5cd0ce6e762c4c0383dddd7445`. No Agent execution/refresh/mutation/remediation authority |")
          replace_once(
              catalog,
              "\n\n## BATCH-200 Enterprise Operations Expansion",
              "\n| Bounded cached Performance Health estate export | BATCH-800 / B800-089 | In validation / PR #324 | Viewer+ estate-wide `GET /reports/performance-health.csv` reads enabled registrations plus snapshot-cache `Peek(...)`, exports current active/runnable/pending-I/O aggregates and reuses anonymous top `WaitIntelligenceProjection` category/score/severity/rate/share/signal evidence through `monitor-export-v2`. Missing cache/performance/wait evidence remains `Unavailable`, observed workload zeroes remain zero, and concrete wait type/fingerprint plus registration IDs are excluded. Implementation head `01dd1780c5b84137a41bb4e262bc36a767bc1bde` passed CI #2790 / `32081864141` and Windows production-candidate #513 / `32081864132`; Real SQL was not selected because no monitored-SQL query/collector/permission path changed. No wait reset/tuning/refresh/mutation/remediation authority |\n\n## BATCH-200 Enterprise Operations Expansion")

          status = "docs/STATUS.md"
          replace_once(
              status,
              "PR #323 carries B800-088. Viewer+ estate-wide `GET /reports/sql-agent-health.csv` reads enabled registration/control-plane state plus `IServerHealthSnapshotCache.Peek(...)` only and serializes aggregate total/enabled/failed-last-run counts, explicit history/activity availability, anonymous highest existing `AgentReliabilityProjection.Build(jobs, 1)` risk metrics and current activity/running counts through `monitor-export-v2`. Missing cache/history/activity evidence remains explicit `Unavailable`; legitimate observed aggregate zeroes remain numeric zeroes. `ScheduleLatenessState` is `NotEvaluated` when activity evidence exists because server-local next-run evidence lacks canonical server time-zone + recurrence/expected-run semantics. Job keys/names, owners, individual next-run timestamps and registration IDs are excluded. Implementation head `ea87e414f97060e9156fd762d407c8e815c26103` passed CI #2772 / `32079766599` and Windows production-candidate #508 / `32079766682`; Real SQL was not selected because no monitored-SQL query, collector or permission path changed. No Agent execution, refresh, schedule/job mutation, remediation, failover, maintenance action or configuration write is introduced.\n\n**Ready/merge for B800-088 requires all four canonical files reconciled and every repository-selected required workflow Green on the same exact final post-reconciliation head**, no unresolved review threads, branch current with `main`, and a diff bounded to B800-088 implementation/tests/work-note/canonical reconciliation. Real SQL is required only when repository path policy selects it. #287 remains OPEN for B800-089+.",
              "B800-088 is complete and merged through PR #323 as `88bbd3bf22c99a5cd0ce6e762c4c0383dddd7445`. Viewer+ estate-wide `GET /reports/sql-agent-health.csv` reads enabled registration/control-plane state plus `IServerHealthSnapshotCache.Peek(...)` only and serializes aggregate total/enabled/failed-last-run counts, explicit history/activity availability, anonymous highest existing `AgentReliabilityProjection.Build(jobs, 1)` risk metrics and current activity/running counts through `monitor-export-v2`. Missing cache/history/activity evidence remains explicit `Unavailable`; legitimate observed aggregate zeroes remain numeric zeroes. `ScheduleLatenessState` is `NotEvaluated` when activity evidence exists because server-local next-run evidence lacks canonical server time-zone + recurrence/expected-run semantics. Job keys/names, owners, individual next-run timestamps and registration IDs are excluded. Exact final reconciled head `b612e2f5d0a0194df39fcea235ef0d5882e2873a` passed CI #2780 / `32080772859`, Real SQL #362 / `32080772786`, and Windows production-candidate #512 / `32080772801`. No Agent execution, refresh, schedule/job mutation, remediation, failover, maintenance action or configuration write was introduced.\n\nPR #324 carries B800-089. Viewer+ estate-wide `GET /reports/performance-health.csv` reads enabled registrations plus `IServerHealthSnapshotCache.Peek(...)` only and serializes current active requests, runnable tasks and pending I/O aggregates plus the anonymous highest existing B400 wait category/score/severity/rate/share/signal summary from `WaitIntelligenceProjection.Build(performance, uptime, 1)`. Missing cache/performance/wait evidence remains explicit `Unavailable`; legitimate observed workload zeroes remain numeric zeroes. Concrete wait type/fingerprint and registration IDs are excluded, and the wait evidence remains cumulative since SQL Server start and uptime-normalized rather than interval history. Implementation head `01dd1780c5b84137a41bb4e262bc36a767bc1bde` passed CI #2790 / `32081864141` and Windows production-candidate #513 / `32081864132`; Real SQL was not selected because no monitored-SQL query, collector or permission path changed. No wait reset, tuning, refresh, mutation, remediation, failover, maintenance action or configuration write is introduced.\n\n**Ready/merge for B800-089 requires all four canonical files reconciled and every repository-selected required workflow Green on the same exact final post-reconciliation head**, no unresolved review threads, branch current with `main`, and a diff bounded to B800-089 implementation/tests/work-note/canonical reconciliation. Real SQL is required only when repository path policy selects it. #287 remains OPEN for B800-090+.")

          plan = "docs/IMPLEMENTATION_PLAN.md"
          replace_once(
              plan,
              "- B800-088 exposes Viewer+ estate-wide `GET /reports/sql-agent-health.csv` through enabled registration/control-plane state plus `IServerHealthSnapshotCache.Peek(...)` and reuses `AgentReliabilityProjection.Build(jobs, 1)` for anonymous highest-risk reliability metrics;\n- B800-088 exports aggregate total/enabled/failed-last-run counts, explicit history/activity availability and current activity/running counts only; missing cache/history/activity evidence remains `Unavailable`, valid observed aggregate zeroes remain zero, and schedule lateness remains `NotEvaluated` because canonical server time-zone + recurrence/expected-run semantics are not collected;\n- B800-088 excludes job keys/names, owners, individual next-run timestamps and registration IDs and adds no monitored-SQL query/collector/permission, refresh, Agent execution, schedule/job mutation, remediation, failover or configuration-write path;\n- B800-088 implementation head `ea87e414f97060e9156fd762d407c8e815c26103` passed CI #2772 / `32079766599` and Windows production-candidate #508 / `32079766682`; Real SQL was not selected because no monitored-SQL query, collector or permission path changed. Exact final post-canonical-reconciliation validation remains required.",
              "- B800-088 is complete and merged through PR #323 as `88bbd3bf22c99a5cd0ce6e762c4c0383dddd7445`; exact final reconciled head `b612e2f5d0a0194df39fcea235ef0d5882e2873a` passed CI #2780 / `32080772859`, Real SQL #362 / `32080772786`, and Windows production-candidate #512 / `32080772801`;\n- B800-088 exposes Viewer+ estate-wide `GET /reports/sql-agent-health.csv` through enabled registration/control-plane state plus `IServerHealthSnapshotCache.Peek(...)`, reuses anonymous `AgentReliabilityProjection.Build(jobs, 1)` metrics, preserves missing history/activity as `Unavailable`, keeps schedule lateness `NotEvaluated`, excludes job keys/names/owners/next-run timestamps/registration IDs, and adds no monitored-SQL/refresh/Agent execution/mutation path;\n- B800-089 exposes Viewer+ estate-wide `GET /reports/performance-health.csv` through enabled registration/control-plane state plus `IServerHealthSnapshotCache.Peek(...)` and reuses `WaitIntelligenceProjection.Build(performance, uptime, 1)` for anonymous highest existing B400 wait category/score/severity/rate/share/signal evidence;\n- B800-089 exports current active requests, runnable tasks and pending I/O aggregates only; missing cache/performance/wait evidence remains `Unavailable`, valid observed workload zeroes remain zero, concrete wait type/fingerprint and registration IDs are excluded, and cumulative wait evidence is not represented as interval history;\n- B800-089 implementation head `01dd1780c5b84137a41bb4e262bc36a767bc1bde` passed CI #2790 / `32081864141` and Windows production-candidate #513 / `32081864132`; Real SQL was not selected because no monitored-SQL query, collector or permission path changed. Exact final post-canonical-reconciliation validation remains required.")
          replace_once(
              plan,
              "Least privilege remains read-only: SQL Server 2022+ uses `VIEW SERVER PERFORMANCE STATE` (older supported versions `VIEW SERVER STATE`) plus `VIEW ANY DEFINITION` and existing narrow metadata grants; Agent history/activity adds only read-only `SELECT` on `msdb.dbo.sysjobhistory` and `msdb.dbo.sysjobactivity`, with no SQLAgent execution/operator role. B800-071/072/073/074/075/076/077/078/079/080/081/082/083/084/085/086/087/088 add no monitored-SQL permission or query path.\n\nPR #323 becomes eligible for Ready/merge only after `BATCH_800`, `FEATURE_CATALOG`, `STATUS` and this plan are reconciled on one exact head, every repository-selected required workflow is Green on that same head, review threads are resolved, the branch is current with `main`, and the effective diff remains bounded to B800-088 plus canonical reconciliation. Real SQL is required only if repository path policy selects it. Merging #323 closes B800-088 only; #287 remains OPEN for B800-089+.",
              "Least privilege remains read-only: SQL Server 2022+ uses `VIEW SERVER PERFORMANCE STATE` (older supported versions `VIEW SERVER STATE`) plus `VIEW ANY DEFINITION` and existing narrow metadata grants; Agent history/activity adds only read-only `SELECT` on `msdb.dbo.sysjobhistory` and `msdb.dbo.sysjobactivity`, with no SQLAgent execution/operator role. B800-071/072/073/074/075/076/077/078/079/080/081/082/083/084/085/086/087/088/089 add no monitored-SQL permission or query path.\n\nPR #324 becomes eligible for Ready/merge only after `BATCH_800`, `FEATURE_CATALOG`, `STATUS` and this plan are reconciled on one exact head, every repository-selected required workflow is Green on that same head, review threads are resolved, the branch is current with `main`, and the effective diff remains bounded to B800-089 plus canonical reconciliation. Real SQL is required only if repository path policy selects it. Merging #324 closes B800-089 only; #287 remains OPEN for B800-090+.")
          replace_once(
              plan,
              "- `docs/BATCH_800.md` — B800-001..100 IN PROGRESS under #287; incremental focused slices are merged through B800-087 on `main`, with PR #323 carrying B800-088. The overall batch is not counted as complete.",
              "- `docs/BATCH_800.md` — B800-001..100 IN PROGRESS under #287; incremental focused slices are merged through B800-088 on `main`, with PR #324 carrying B800-089. The overall batch is not counted as complete.")

          print("B800-089 canonical reconciliation completed with guarded one-match replacements.")
          PY

          git diff --check
          git diff -- docs/BATCH_800.md docs/FEATURE_CATALOG.md docs/STATUS.md docs/IMPLEMENTATION_PLAN.md
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/BATCH_800.md docs/FEATURE_CATALOG.md docs/STATUS.md docs/IMPLEMENTATION_PLAN.md
          git commit -m "B800-089: reconcile canonical tracking"
          git push origin HEAD:agent/b800-089-performance-health-export
