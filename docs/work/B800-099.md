# B800-099 — cross-document exact-head consistency gate

## Objective

Lock the final pre-closeout BATCH-800 repository state across the four canonical documents after B800-098 merged. This slice performs no production behavior change and does not yet declare BATCH-800 complete.

The required canonical state before B800-100 is:

- Issue #287 remains OPEN.
- BATCH-800 remains `IN PROGRESS`.
- Focused B800 slices through B800-098 are merged.
- B800-098 / PR #333 exact final head `e2c33c85becb40d4f0f639a178dd435c37578e01` passed CI #2908 / `32089818839`, Real SQL #376 / `32089818822`, and Windows production-candidate #544 / `32089818821` Green.
- PR #333 squash-merged as `483b8ce2f14b62499d8751d22f1908511f981d10`.
- B800-099..100 remain pending: B800-099 owns this exact-head canonical consistency gate; B800-100 owns final repository batch closeout.

## Canonical documents

The exact pre-closeout state must be represented consistently in:

1. `docs/BATCH_800.md` — authoritative task ledger;
2. `docs/STATUS.md` — current project status summary;
3. `docs/FEATURE_CATALOG.md` — feature-level status catalog;
4. `docs/IMPLEMENTATION_PLAN.md` — canonical execution plan.

No document may still present B800-098 as pending after PR #333 merged, and none may declare BATCH-800 complete before B800-100.

## Diagnostic truth boundary

The cross-document gate also preserves the current evidence distinction:

- bounded TempDB, transaction-log and HA evidence is collected/projected;
- unsupported composite growth/contention, governed recovery and quorum/RPO/RTO/failover-readiness conclusions remain `NotEvaluated`/unavailable where inputs are absent;
- query regression remains a privacy-safe contract only, with no live query-regression collection, SQL text or query-plan collection.

## Safety / production boundary

- Documentation and regression tests only.
- No controller, route, RBAC, service, collector, monitored-SQL query, permission, cache, deployment, mutation, remediation, tuning or failover change.
- No positive multi-replica AG acceptance claim.
- No RC.61 publication/selection change.
- No real production IIS/SQL mutation or external acceptance evidence is created.
- B800-099 cannot satisfy, bypass or alter `#162 -> #116 -> #111`.

## B800-100 entry condition

B800-100 may begin only after this branch has one settled exact head, every repository-selected workflow is Green on that same head, the branch is current with `main`, the effective diff remains bounded to canonical consistency/tests/docs, and review threads are resolved. Only then may final repository BATCH-800 closeout be considered; external production acceptance remains independent.
