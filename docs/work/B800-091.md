# B800-091 — reports tranche cross-layer acceptance contract

## Objective

Begin B800-091..100 final acceptance/closeout work without adding a new production feature. Lock the completed B800-081..090 reports/exports tranche as one cross-layer contract so future route, RBAC, discoverability or collection-boundary drift fails CI rather than being found manually.

## Scope

- Add one focused acceptance test matrix covering the nine Viewer+ report routes delivered by B800-081..090.
- Require each route to keep its exact `HttpGet` template and effective controller-level `Monitor.Read` policy with no action-level Manage override.
- Keep administrator-only Audit and diagnostics Manifest as explicit negative controls requiring `Monitor.Manage` action overrides.
- Require the five estate/global tranche exports to remain discoverable exactly once in the standard Reports section.
- Require contextual Server Intelligence, Database Health and Memory Health selection workflows to remain visible without turning them into unscoped global downloads.
- Keep Audit/Manifest links out of the standard Viewer+ section and inside the Administrator section.
- Lock the web/service layer against direct `ISqlSnapshotQuery`, `ISqlServerSnapshotCollector`, `ISnapshotRefreshService` or `SqlConnection` dependencies.
- Retain the tranche truth language for `Unavailable`, `NotEvaluated`, allocation-vs-capacity and no browser-triggered monitored-SQL collection.

## Why this is not duplicate coverage

B800-087..090 export tests validate individual serialization and redaction contracts. Earlier B800 navigation tests validate bounded GET navigation. B800-091 adds the missing tranche-level parity check across all completed report routes: route template + RBAC + Reports discoverability + no direct collection/refresh dependency in one acceptance surface.

## Safety / behavior boundary

- Test/documentation-only implementation slice.
- No production controller, service, collector, query, permission, route, UI behavior or storage mutation change.
- No monitored-SQL access is added.
- No new remediation, tuning, failover, maintenance or configuration authority.
- No change to RC.61 publication/selection or `#162 -> #116 -> #111` production acceptance.

## Implementation files

- `tests/Monitor.Web.Tests/B800ReportTrancheAcceptanceTests.cs`
- `docs/work/B800-091.md`

## Implementation-head validation

Implementation head `7d6b58ba8383412cafeec1228f88e9bcae1a3eda` passed CI #2824 / run `32083874505` — **Green**, including Release build, the full test suite and repository safety tooling. The new cross-layer report acceptance matrix passed on the completed B800-081..090 production surface.

Real SQL and Windows production-candidate were not selected on this implementation head because B800-091 changes only tests/documentation and no production, monitored-SQL, permission, packaging or runtime path.

## Remaining merge gate

Canonical `docs/BATCH_800.md`, `docs/FEATURE_CATALOG.md`, `docs/STATUS.md` and `docs/IMPLEMENTATION_PLAN.md` still require B800-091 reconciliation. After reconciliation, every repository-selected workflow must be Green on one exact final head, the branch must remain current with `main`, review threads must be resolved, and the effective diff must remain bounded to the two B800-091 implementation/work-note files plus the four canonical files.

Merging B800-091 closes this acceptance-contract slice only. Issue #287 remains OPEN for B800-092..100 final acceptance/closeout work.
