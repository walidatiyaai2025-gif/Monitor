# B800-095 — canonical closeout ledger guard

## Objective

Start the final B800 canonical closeout without changing production behavior. Before rewriting the large canonical ledgers, capture the exact post-B800-094 merge baseline, identify stale unchecked task rows that already have merged implementation evidence, and add a fail-closed regression guard so later reconciliation cannot silently overstate completion.

## Post-B800-094 baseline

PR #329 merged B800-094 into `main` as `67ee71224708153eecc31cf495148ffff00f50dc` after exact final reconciled head `64488940674a39304010901cb87c2025ba3376a9` passed the complete selected gate set:

- CI run `32086916585` — Green.
- Real SQL acceptance run `32086916571` — Green.
- Windows production-candidate run `32086916578` — Green end-to-end.

Issue #287 remains open. BATCH-800 remains `IN PROGRESS`; this slice must not manufacture a `COMPLETE` state.

## Historical ledger drift identified

The following task rows are still unchecked in `docs/BATCH_800.md`, but repository history contains explicit merged implementation commits for them. B800-095 records that discrepancy only; B800-096 must perform the deliberate canonical rewrite and preserve the exact evidence links.

| Task | Existing merged evidence | Closeout interpretation |
|---|---|---|
| `B800-020` | `0d9c05d6c3c2b2980a6c3c8bbfbe241dc305860a` — governed snapshot evidence parity; exact implementation head passed CI #2285, Real SQL #267 and Windows production-candidate #366 | implementation evidence exists; canonical checkbox is stale |
| `B800-065` | `d831f77159e43b446aa7549db5a6d74cd23a3f0e` — bounded TempDB snapshot evidence | implementation evidence exists; canonical checkbox is stale |
| `B800-066` | `fad66ef563300f0aaedf8fad472b377ca55db648` — bounded transaction-log snapshot evidence | implementation evidence exists; canonical checkbox is stale |
| `B800-067` | `1b9518e7ceb813368106f5a04483817414f047b1` — bounded HA readiness evidence | implementation evidence exists; canonical checkbox is stale |
| `B800-068` | `8e5aea353b8255849b2c82675ec8f0b5443e88db` — privacy-safe query-regression evidence contract | contract evidence exists; live collection remains deliberately outside that task's implemented scope |
| `B800-070` | `3073c3a5b4b802b24a3b59218ee93e1208f534a3` / PR #302 — bounded diagnostics projected into cached operator surfaces | implementation evidence exists; canonical checkbox is stale |

Source-level acceptance coverage also confirms that TempDB, transaction-log and HA evidence are bounded, fail-closed and cache-only on browser reads, and that specialized operator surfaces consume the shared cached diagnostics without adding browser SQL. Query-regression evaluation remains a privacy-safe interval contract over opaque engine hashes/counters and is not presented as live query text/plan collection.

## Items deliberately not reconciled by B800-095

`B800-039`, `B800-040`, `B800-050` and `B800-062` are validation/closeout rows rather than uniquely attributable implementation commits. Later successful repository-wide exact-head gates may be sufficient evidence, but B800-095 does not silently reinterpret those historical requirements. B800-096 must make that evidence mapping explicit before changing those rows.

The final B800-095..100 sequence therefore remains fail-closed: a later slice must reconcile stale task rows, then run one exact-head canonical gate set and only then consider BATCH-800 closure.

## Acceptance guard

`B800CloseoutLedgerGuardTests` locks four things before the canonical rewrite:

1. BATCH-800 remains `IN PROGRESS` and the current `B800-095..100` final-closeout marker remains pending.
2. The six stale implementation rows above remain visibly unchecked until a dedicated reconciliation changes both ledger and guard together.
3. B800-094 final head, merge commit and three selected Green runs are preserved as the next closeout baseline.
4. Historical validation-only rows remain open until B800-096 supplies a deliberate evidence mapping.

This intentionally makes canonical status changes reviewable rather than allowing a broad documentation edit to erase unresolved distinctions.

## Safety / production boundary

- Tests/documentation only; no runtime source change.
- No controller, route, authorization, service, collector, SQL query, cache, permission or deployment change.
- No synthetic evidence, no conversion of missing evidence to zero/healthy, and no unsupported diagnostic readiness claim.
- No RC.61 publication/selection mutation.
- B800-095 does not satisfy or bypass `#162 -> #116 -> #111` and does not imply real trusted-IIS production acceptance.

## Next slice

B800-096 should reconcile the stale canonical task rows against the evidence recorded here, explicitly decide the validation-only rows (`B800-039`, `B800-040`, `B800-050`, `B800-062`), update the canonical B800 status surfaces together, and then run the repository-selected exact-head gate set on the reconciled branch.
