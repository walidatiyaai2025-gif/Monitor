# B800-096 — legacy closeout evidence mapping

## Objective

Convert the stale-task inventory captured by B800-095 into an explicit, reviewable evidence map before changing the large canonical BATCH-800 checklist. This slice changes no runtime behavior and does not yet flip the historical checkboxes. It establishes which merged exact-head evidence is sufficient for B800-020, B800-039, B800-040, B800-050, B800-062, B800-065, B800-066, B800-067, B800-068 and B800-070.

The actual checklist rewrite is intentionally isolated to B800-097 so the evidence decision and the broad canonical edit are independently reviewable.

## Evidence mapping

### B800-020 — first vertical slice closeout

PR #294 restored the production-DI governed snapshot reader parity and explicitly closes the B800-020 runtime validation gap. Exact head `ca065f32ba64d8a8d1c7b909cecd6a342aa0be7c` passed CI #2285, Real SQL #267 and Windows production-candidate #366 before squash merge `0d9c05d6c3c2b2980a6c3c8bbfbe241dc305860a`.

**Decision:** sufficient merged evidence exists to mark B800-020 complete during B800-097 canonical reconciliation.

### B800-039 / B800-040 / B800-050 / B800-062 — diagnostic exact-head validation and closeout

PR #288 was merge-frozen to B800-031..063 (plus the earlier operator/workflow scope and B800-069), including the memory, wait/file-I/O and SQL Agent diagnostic slices represented by these validation-only task rows. Its final settled head `7d2f0d7caa713b95bf7bc5666c9056c5c22055a8` was current with `main`, had zero unresolved review threads, and passed the full exact-head gate set:

- CI #2245 — Green;
- Real SQL acceptance #265 — Green on SQL Server 2022 / least privilege;
- Windows production-candidate #361 — Green.

PR #288 then squash-merged as `54fac01a1ed2ce7eb06f94b7de7d3681da75ac6d`.

Because the final PR scope explicitly includes B800-031..063 and the final merge gate explicitly includes canonical reconciliation plus all three selected validation paths, this is direct closeout evidence rather than a later unrelated Green run.

**Decision:** B800-039, B800-040, B800-050 and B800-062 may all be marked complete in B800-097 with PR #288 as the common exact-head evidence owner.

### B800-065 — bounded TempDB evidence

PR #295 implemented the bounded TempDB snapshot slice. Implementation head `524c13096c72c383ced88c7b9f85fc12c501de83` passed CI #2305, Real SQL #273 and Windows production-candidate #372; the reconciled branch head was `cab1437ca2aa01500d57c12ab2aa5b3b1e1c9d96`, and PR #295 squash-merged as `d831f77159e43b446aa7549db5a6d74cd23a3f0e`.

**Decision:** sufficient merged evidence exists to mark B800-065 complete.

### B800-066 — bounded transaction-log evidence

PR #296 final exact head `b1101db0c3d9b181b6899fc84c7ddb4c6bcb87ea` passed CI #2319, Real SQL #276 and Windows production-candidate #375, was behind 0 with no unresolved review threads, and squash-merged as `fad66ef563300f0aaedf8fad472b377ca55db648`.

**Decision:** sufficient merged evidence exists to mark B800-066 complete.

### B800-067 — bounded HA evidence

PR #297 final exact head `0bb395fff9107fe993f3a804784ae33afa870212` passed CI #2335, Real SQL #277 and Windows production-candidate #376, was behind 0 with no unresolved review threads, and squash-merged as `1b9518e7ceb813368106f5a04483817414f047b1`.

The current acceptance target proves truthful non-HA/empty behavior; it does not claim a positive multi-replica AG integration environment. That limitation is part of the implemented task boundary rather than a reason to leave B800-067 open.

**Decision:** sufficient merged evidence exists to mark B800-067 complete while retaining the explicit future positive-AG acceptance gap.

### B800-068 — privacy-safe query-regression contract

PR #300 defined the contract-only evidence shape and deliberately excluded live query collection. Final synchronized head `d803575418ce44e15d06248c4827eae17633100b` passed CI #2350 and Windows production-candidate #379; Real SQL was correctly not applicable because the slice added no monitored-SQL collector/query/permission path. It squash-merged as `8e5aea353b8255849b2c82675ec8f0b5443e88db`.

**Decision:** B800-068 is complete as a contract-evaluation task. Live query-regression collection remains explicitly outside its scope.

### B800-070 — project evidence-backed diagnostics into operator surfaces

PR #302 final head `56838b67ae44944e146a50921b8802339431b7af` passed CI #2381, Real SQL #291 and Windows production-candidate #391, was behind 0 with no unresolved review threads, and squash-merged as `3073c3a5b4b802b24a3b59218ee93e1208f534a3`.

**Decision:** sufficient merged evidence exists to mark B800-070 complete.

## B800-095 state

B800-095 itself is complete via PR #330. Exact head `d026457b2a7bb9f1b43c2f85c47cf01b1c33d7ec` passed CI #2887 / run `32088182623` and squash-merged as `cda21b6ef5bbb8e34d32a186f44b3e45dc83bb23`.

B800-097 should therefore convert the B800 final-acceptance marker from `B800-095..100` to `B800-096..100`/later as appropriate and reconcile the ten historical task rows above, while keeping the overall batch `IN PROGRESS` until the remaining final acceptance slices finish.

## Safety / production boundary

- Documentation and regression acceptance only; no runtime file is changed.
- No browser-to-SQL path, collector, permission, deployment, mutation, remediation or AI execution path is added.
- Existing missing-evidence / `Unavailable` / `NotEvaluated` truth boundaries remain authoritative.
- No RC.61 publication or selection change.
- This mapping does not satisfy, bypass or alter `#162 -> #116 -> #111` and does not imply real trusted-IIS production acceptance.

## Next slice

B800-097: perform the mechanical canonical checklist reconciliation from this evidence map, update the closeout regression guard to require the reconciled state, and run repository-selected exact-head validation on that bounded documentation/test diff.
